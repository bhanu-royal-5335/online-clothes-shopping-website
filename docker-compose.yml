version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rainbow_fashions_saas
    restart: always
    ports:
      - '5000:5000'
    environment:
      - PORT=5000
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://rainbowadmin:Bhanu5335@cluster0.czkbisg.mongodb.net/ecommerce?retryWrites=true&w=majority&appName=Cluster0
      - CLIENT_URL=https://rainbow-fashions.onrender.com,https://online-clothes-shopping-website.onrender.com
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:5000/api/health']
      interval: 30s
      timeout: 10s
      retries: 3
